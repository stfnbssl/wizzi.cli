$group

    $
        var items = [
            'a_cb',
            'a_cb_throw',
            'a_cb_no_throw',
            'a_reject',
            'a_then',
            'a_then_catch_reject',
            'catch_reject',
            'r_cb',
            'r_cb_err',
            'r_promise',
        ];

    $foreach item in items
        $file __dot__wizzi/${pkgPath}t/${item}.js.ittf.ittf
            ${"${'$'}"}group
                ${'$'}include wz/v08/js/tfolder/${item}.js