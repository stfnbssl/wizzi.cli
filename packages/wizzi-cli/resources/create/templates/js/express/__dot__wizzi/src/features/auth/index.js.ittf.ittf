module
    kind es6
    $
        var call = '(&md)';
    
    ${'$'}
        var md = {
        }

    ${'$'}if wzCtx.Build.useAuthentication || wzCtx.Build.useAuthorization
        v08/express/features/auth/index${call}
