module
    kind es6
    $
        var call = '(&md)';
    
    ${'$'}
        var md = {
        }

    ${'$'}if wzCtx.Build.useAuthentication
        v08/express/features/auth/controllers/authentication${call}
