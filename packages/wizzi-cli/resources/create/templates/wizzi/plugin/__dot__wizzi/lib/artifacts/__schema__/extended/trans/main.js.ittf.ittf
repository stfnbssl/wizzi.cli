module
    kind es6

    $
        var call = '(&md)'
    
    ${'$'}
        var md = {
            plugin: '${cliCtx.name}',
            name: 'extended',
            schema: '${cliCtx.Schemas[0].name}',
            checkSchema: true
        };

    v08/lib/artifacts/trans${call}      