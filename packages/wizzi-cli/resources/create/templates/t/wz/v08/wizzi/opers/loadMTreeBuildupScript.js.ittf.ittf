$group

    $ 
        var openPar = '(';

    function loadMTreeBuildupScript
        param ittfDocumentUri
        param context
        param callback

        _ createWizziFactory
            {
            f_cb${openPar} wf )
                _ wf.loadMTreeBuildupScript
                    @ ittfDocumentUri
                    @ context
                    @ callback