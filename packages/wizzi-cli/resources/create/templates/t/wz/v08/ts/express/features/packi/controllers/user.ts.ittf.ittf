$group

    $
        var call_md = '(&md)';

    ${'$'}
        var schemas = wzCtx.Build.mongoDb.schemas
        function getSchema(name) {
            for (var i=0; i<schemas.length; i++) {
                if (schemas[i].name == name) { return schemas[i]; }
            }
        }
        var md = getSchema('user')

    v08/mongo/modelcontroller${call_md}

