module
    kind es6
    $
        var call = '(&md)'
    
    ${'$'}
        var md = {
            name: 'blog'
        }

    ${'$'}if wzCtx.Build.useWebpackApp
        v08/express/site/webpacks/blog/webpack.config${call}