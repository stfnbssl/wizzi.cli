module
    kind es6
    $
        var call = '(&md)'
    
    ${'$'}
        var md = {
        }

    ${'$'}if wzCtx.Build.usePassportAuthentication
        v08/express/middlewares/passportAuth0${call}
