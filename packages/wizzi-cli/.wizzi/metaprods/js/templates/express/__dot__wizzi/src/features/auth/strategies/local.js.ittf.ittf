module
    kind es6
    $
        var call = '(&md)';
    
    ${'$'}
        var md = {
        }

    ${'$'}if wzCtx.Build.useAuthentication
        ${'$'}if wzCtx.Build.authentication.useLocal
            v08/express/features/auth/strategies/local${call}
