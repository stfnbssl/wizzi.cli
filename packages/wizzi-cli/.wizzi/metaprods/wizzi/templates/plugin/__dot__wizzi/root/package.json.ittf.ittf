{
    $
        var call = '(&md)'
    
    ${'$'}
        var md = {
        }

    v08/root/package${call}

        ${'$'}append scripts
            examples "node examples"
            docx "node examples/dist/first.${cliCtx.Schemas[0].name}"

        ${'$'}append dependencies
            docx "^5.5.0"

        ${'$'}append devDependencies