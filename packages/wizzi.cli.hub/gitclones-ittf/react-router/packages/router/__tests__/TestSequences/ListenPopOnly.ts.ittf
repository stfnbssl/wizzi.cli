module
    :import-type 
        @ History
        from "../../history"
    export-default 
        function ListenPopOnly
            param history
                :ref History
            let spy = jest.fn()
            let unlisten = history.listen(spy)
            _ history.push("/2")
            _ expect(history.location.pathname).toBe("/2")
            _ history.replace("/3")
            _ expect(history.location.pathname).toBe("/3")
            _ expect(spy).not.toHaveBeenCalled()
            _ unlisten()
