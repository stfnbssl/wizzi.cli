module
    :import-type 
        @ History
        from "../../history"
    export-default 
        function PushMissingPathname
            param history
                :ref History
            _ expect(history.location).toMatchObject
                { 
                    @ pathname "/"
            _ history.push("/home?the=query#the-hash")
            _ expect(history.action).toBe("PUSH")
            _ expect(history.location).toMatchObject
                { 
                    @ pathname "/home"
                    @ search "?the=query"
                    @ hash "#the-hash"
            _ history.push("?another=query#another-hash")
            _ expect(history.action).toBe("PUSH")
            _ expect(history.location).toMatchObject
                { 
                    @ pathname "/home"
                    @ search "?another=query"
                    @ hash "#another-hash"
