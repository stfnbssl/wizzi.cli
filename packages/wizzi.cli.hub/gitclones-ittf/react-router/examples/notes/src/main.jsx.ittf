module
    kind react
    import 
        as React
        from "react"
    import 
        as ReactDOM
        from "react-dom/client"
    import App from "./app"
    _ ReactDOM.createRoot(document.getElementById("root")).render
        < React.StrictMode 
            < App 
